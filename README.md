📝 To-Do App

A small to-do list project made with HTML, CSS, JS, PHP and MySQL.
You can add tasks, mark them as done, and delete them.
Everything is saved in a database so it stays even after refresh.

🔧 What We used

HTML + CSS for the UI

JavaScript (fetch) for sending requests

PHP for backend

MySQL (XAMPP) for storing tasks

📌 Features

Add tasks

Mark tasks as completed (one-way checkbox)

Strike-through for completed tasks

Delete tasks

Data stored in MySQL
